# Stage 4 Knowledge and Decision / v2.2.2

Source: TEST PostgreSQL + MOCK providers. Real provider calls: 0.

Command:
`PYTHONPATH=backend .venv/bin/python scripts/test_postgres.py .venv/bin/pytest --junitxml=evidence/stage4-postgres.xml`

Result: 123 passed. Initial failures: stage4-red.xml; targeted run: stage4-green.xml.

Verified branches: above threshold hold; applicable rain defer before urgent override;
within-window deficit water; severe deficit outside-window water; remaining deficit
defer; hard gates hold; deterministic outputs; bounded temperature/humidity/pot/memory
adjustments; low-drainage reductions; no moisture-to-volume linear formula.

LLM schemas reject execution fields. Care research stores two labelled source records,
shows conflicts, and falls back to an unconfirmed conservative knowledge template on
invalid LLM output. Weather failure removes weather adjustment. HTTP provider calls use
independent HTTPS configuration, timeouts, two attempts and persistent daily budgets.
The real adapter expects a normalized provider gateway; no upstream account is assumed.

Care and memory jobs commit artifacts with the fenced job result. User confirmation
precedes policy generation and memory enabling. Mock memory effects generate labelled
events but never increment the real-use counter. Policy compile output validates with
the Pi schema and canonical hash, including IANA timezone and seven-day validity.
Auto-mode state is returned outside the policy envelope and persisted by Pi; local
fallback requires explicit enabled state. This cannot deliver new user settings to an
already offline device; the bounded cached-policy/offline timeout remains the limit.

Actual horticultural provider responses, real memory use, Pi fallback pumping and
WeChat deployment remain NOT_RUN/BLOCKED.
