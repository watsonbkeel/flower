<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="0" failures="1" skipped="0" tests="1" time="0.591" timestamp="2026-09-13T17:12:42.883693+08:00" hostname="VM-0-13-debian"><testcase classname="backend.tests.test_full_mock_flow" name="test_full_offline_demo_uses_both_gates_and_persists_actual_mock_result" time="0.475"><failure message="ValueError: RECEIPT_MISMATCH">system = {'app': &lt;fastapi.applications.FastAPI object at 0x7faeaeb2c1a0&gt;, 'client': &lt;starlette.testclient.TestClient object at ...E3ODkyOTI1NjN9.AFwZ_RMMCfxqtUx2_xdK_Sv99hT3TjI_7OgoHHjXsaQ'}, 'device_id': 'c09dc7d7-d956-4553-ab97-5a9119fe1250', ...}
tmp_path = PosixPath('/tmp/pytest-of-root/pytest-45/test_full_offline_demo_uses_bo0')

    def test_full_offline_demo_uses_both_gates_and_persists_actual_mock_result(system, tmp_path):
        client, headers, device_headers = (
            system["client"],
            system["user_headers"],
            system["device_headers"],
        )
        pid = system["plant_id"]
        system["settings"].run_mode = "offline_demo"
        worker = Worker(system["app"].state.sessions, system["settings"])
        uploaded = client.post(
            "/api/v1/device/images",
            headers=device_headers,
            data={"plant_id": pid, "image_type": "whole"},
            files={"file": ("sample.jpg", Path("miniapp/assets/plant.jpg").read_bytes(), "image/jpeg")},
        )
        assert uploaded.status_code == 202
        assert worker.run_once()
        recognition = client.get(f"/api/v1/plants/{pid}/recognition", headers=headers).json()
        candidate = recognition["result"]["candidates"][0]
        assert recognition["result"]["source_type"] == "mock"
        assert (
            client.post(
                f"/api/v1/plants/{pid}/confirm-species",
                headers=headers,
                json={
                    "common_name": candidate["common_name"],
                    "scientific_name": candidate["scientific_name"],
                    "input_method": "recognition",
                    "confidence": candidate["confidence"],
                },
            ).status_code
            == 200
        )
        assert (
            client.post(f"/api/v1/plants/{pid}/care-profile/generate", headers=headers).status_code
            == 202
        )
        assert worker.run_once()
        with system["app"].state.sessions() as db:
            profile = db.scalar(
                select(CareProfile)
                .where(CareProfile.plant_id == pid)
                .order_by(CareProfile.version.desc())
            )
            profile_id = profile.id
        assert (
            client.post(
                f"/api/v1/plants/{pid}/care-profile/confirm",
                headers=headers,
                json={"profile_id": profile_id},
            ).status_code
            == 200
        )
        response = client.post(
            f"/api/v1/plants/{pid}/water",
            headers=headers | {"Idempotency-Key": "offline-demo"},
            json={"quantity": 20, "unit": "ml"},
        )
        assert response.status_code == 202
        assert response.json()["status"] == "pending"
        cmd = client.post("/api/v1/device/commands/claim", headers=device_headers).json()["command"]

        class Clock:
            value = 0

            def utcnow(self):
                return datetime.now(timezone.utc)

            def monotonic(self):
                return self.value

            def sleep(self, seconds):
                self.value += seconds

        calibration = Calibration(
            flow_ml_sec=3,
            afterdrip_mean_ml=1,
            afterdrip_max_ml=1,
            min_run_sec=1,
            adc_dry=20000,
            adc_wet=10000,
            insert_depth_mark="MOCK",
            calibrated_at=datetime.now(timezone.utc),
        )
        state = DeviceState(
            operating_mode="FULL",
            activity="IDLE",
            water_level_ok=True,
            soil_pct=20,
            time_trusted=True,
            calibration_valid=True,
            profile_valid=True,
        )
        pump, ledger, clock = MockPump(), Ledger(tmp_path / "pi.db"), Clock()
        executor = Executor(system["device_id"], pump, ledger, lambda: state, calibration, clock)

        def started():
            assert (
                client.post(
                    f"/api/v1/device/commands/{cmd['id']}/started", headers=device_headers
                ).status_code
                == 200
            )

        def progress(pulses):
            assert (
                client.post(
                    f"/api/v1/device/commands/{cmd['id']}/progress",
                    headers=device_headers,
                    json={"pulses": pulses},
                ).status_code
                == 200
            )

        result = executor.execute(decode_command(cmd), on_started=started, on_progress=progress)
        assert result["status"] == "succeeded"
        assert clock.value &gt; 60
        assert pump.starts == 2
        assert not pump.commanded_on
        response = client.post(
            f"/api/v1/device/commands/{cmd['id']}/result", headers=device_headers, json=result
        )
        assert response.status_code == 200
        assert response.json()["status"] == "succeeded"
        reconciliation = client.post('/api/v1/device/quota/reconcile', headers=device_headers,
            json={'used_24h_ml': ledger.used(clock.utcnow()), 'receipts': ledger.receipts()})
        assert reconciliation.status_code == 200
        receipts = reconciliation.json()['verified_receipts']
        assert len(receipts) == 1
&gt;       assert ledger.reconcile_receipt(receipts[0], clock.utcnow())
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

backend/tests/test_full_mock_flow.py:146:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = &lt;flower_pi.storage.ledger.Ledger object at 0x7faead2c4440&gt;
receipt = {'command_id': 'a755c6d7-69b6-4b1d-9b28-1b5fca70ab98', 'result': {'actual_ml': 20.0, 'finished_at': '2026-09-13T09:12:...9:12:43.331296Z', 'pulse': 1}, {'estimated_ml': 10.0, 'finished_at': '2026-09-13T09:12:43.344845Z', 'pulse': 2}], ...}}
now = datetime.datetime(2026, 9, 13, 9, 12, 43, 432722, tzinfo=datetime.timezone.utc)

    def reconcile_receipt(self, receipt, now):
        command_id = receipt["command_id"]
        row = self.get(command_id)
        local = self.value("receipt:" + command_id)
        result = receipt["result"]
        if not local or not row:
            raise ValueError("MISSING_LOCAL_RECEIPT")
        fields = ("status", "actual_ml", "pulses", "finished_at")
        if any(local.get(field) != result.get(field) for field in fields):
&gt;           raise ValueError("RECEIPT_MISMATCH")
E           ValueError: RECEIPT_MISMATCH

pi-agent/flower_pi/storage/ledger.py:164: ValueError</failure></testcase></testsuite></testsuites>