<view class="page"><view class="title">守候记录</view><view class="error" wx:if="{{error}}">{{error}}</view><view class="empty" wx:if="{{!items.length}}">暂无守候记录</view><view class="item" wx:for="{{items}}" wx:key="id"><view class="row"><text class="muted">{{item.time}} UTC</text><text class="source">{{item.label}}</text></view><view class="name">{{item.human_readable}}</view></view></view>
